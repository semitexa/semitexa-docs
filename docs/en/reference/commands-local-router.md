---
id: reference/commands-local-router
section: reference
slug: commands-local-router
title: local-router commands
summary: Every `local-router` command, with the arguments and options the console actually defines.
order: 30
locale: en
status: published
---

<!-- Generated by `bin/semitexa docs:reference:generate` — do not edit by hand. -->

# local-router commands

Every `local-router` command, with the arguments and options the console actually defines.

## `local-router:doctor`

Handled by the `bin/semitexa` wrapper before PHP starts.

## `local-router:init`

Handled by the `bin/semitexa` wrapper before PHP starts.

## `local-router:restart`

Handled by the `bin/semitexa` wrapper before PHP starts.

## `local-router:start`

Handled by the `bin/semitexa` wrapper before PHP starts.

## `local-router:stop`

Handled by the `bin/semitexa` wrapper before PHP starts.

