---
id: reference/commands-skins
section: reference
slug: commands-skins
title: skins commands
summary: Every `skins` command, with the arguments and options the console actually defines.
order: 30
locale: en
status: published
---

<!-- Generated by `bin/semitexa docs:reference:generate` — do not edit by hand. -->

# skins commands

Every `skins` command, with the arguments and options the console actually defines.

## `skins:eval:run`

Run the prompt-resolution eval corpus against the live LLM and report hit rate.

```bash
bin/semitexa skins:eval:run
```

| option | takes a value |
|---|---|
| `--fixtures` | yes |
| `--json` | no |
| `--fail-threshold` | yes |

## `skins:explain-prompt`

Show how the LLM skill resolves a natural-language prompt into skin seed parameters, without generating CSS.

```bash
bin/semitexa skins:explain-prompt <prompt>
```

| option | takes a value |
|---|---|
| `--json` | no |

## `skins:generate`

Generate a semantic skin from a primary color hex (seed mode) or a natural-language prompt (LLM mode). Writes both light and dark mode tokens to src/skins/.

```bash
bin/semitexa skins:generate [algorithm] [hex]
```

| option | takes a value |
|---|---|
| `--prompt` | yes |
| `--name` | yes |
| `--description` | yes |
| `--knob` | yes |
| `--describe` | no |
| `--write` | no |
| `--json` | no |

## `skins:rebuild`

Re-emit tokens.css for a skin from its skin.json (canonical path; works for every skin source).

```bash
bin/semitexa skins:rebuild [slug]
```

| option | takes a value |
|---|---|
| `--all` | no |
| `--write` | no |
| `--json` | no |

## `skins:refine`

Refine an existing skin by adjusting its knobs (LLM or structured). Regenerates both light and dark mode tokens.

```bash
bin/semitexa skins:refine <slug>
```

| option | takes a value |
|---|---|
| `--prompt` | yes |
| `--set` | yes |
| `--as` | yes |
| `--write` | no |
| `--json` | no |

