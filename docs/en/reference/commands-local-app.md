---
id: reference/commands-local-app
section: reference
slug: commands-local-app
title: local-app commands
summary: Every `local-app` command, with the arguments and options the console actually defines.
order: 30
locale: en
status: published
---

<!-- Generated by `bin/semitexa docs:reference:generate` — do not edit by hand. -->

# local-app commands

Every `local-app` command, with the arguments and options the console actually defines.

## `local-app:doctor`

Handled by the `bin/semitexa` wrapper before PHP starts.

## `local-app:list`

Handled by the `bin/semitexa` wrapper before PHP starts.

## `local-app:reconcile`

Handled by the `bin/semitexa` wrapper before PHP starts.

## `local-app:register`

Handled by the `bin/semitexa` wrapper before PHP starts.

## `local-app:remove`

Handled by the `bin/semitexa` wrapper before PHP starts.

