---
id: reference/commands-tasks
section: reference
slug: commands-tasks
title: tasks commands
summary: Every `tasks` command, with the arguments and options the console actually defines.
order: 30
locale: en
status: published
---

<!-- Generated by `bin/semitexa docs:reference:generate` — do not edit by hand. -->

# tasks commands

Every `tasks` command, with the arguments and options the console actually defines.

## `tasks:tick`

Run one task tick: advance automated tasks, complete them at their ETA, and proactively notify the chat.

```bash
bin/semitexa tasks:tick
```

