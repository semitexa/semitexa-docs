---
id: reference/commands-local-domain
section: reference
slug: commands-local-domain
title: local-domain commands
summary: Every `local-domain` command, with the arguments and options the console actually defines.
order: 30
locale: en
status: published
---

<!-- Generated by `bin/semitexa docs:reference:generate` — do not edit by hand. -->

# local-domain commands

Every `local-domain` command, with the arguments and options the console actually defines.

## `local-domain:add`

Handled by the `bin/semitexa` wrapper before PHP starts.

## `local-domain:list`

Handled by the `bin/semitexa` wrapper before PHP starts.

## `local-domain:mode`

Handled by the `bin/semitexa` wrapper before PHP starts.

## `local-domain:remove`

Handled by the `bin/semitexa` wrapper before PHP starts.

